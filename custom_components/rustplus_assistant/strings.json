{
  "config": {
    "step": {
      "user": {
        "title": "Rust+",
        "description": "How would you like to sign in to your Rust+ account?",
        "menu_options": {
          "qr": "Scan a Steam QR code (recommended)",
          "manual": "Paste FCM credentials JSON"
        }
      },
      "qr": {
        "title": "Scan with the Steam Mobile app",
        "description": "Open the Steam Mobile app, scan the code below, and approve the sign-in — then click **Submit**.\n\n{qr_image}"
      },
      "qr_wait": {
        "title": "Signing in…",
        "description": "Waiting for approval and registering for push notifications…"
      },
      "qr_failed": {
        "title": "Sign-in didn't complete",
        "description": "The Steam QR sign-in failed or timed out. Click **Submit** to show a new code and try again."
      },
      "manual": {
        "description": "Paste the FCM credentials JSON from the Rust+ companion browser extension.",
        "data": {
          "fcm_credentials": "FCM Credentials (JSON string)"
        }
      },
      "reconfigure": {
        "title": "Refresh Rust+ credentials",
        "description": "How would you like to refresh your account credentials? Your paired devices are kept.",
        "menu_options": {
          "reconfigure_qr": "Scan a Steam QR code (recommended)",
          "reconfigure_manual": "Paste FCM credentials JSON"
        }
      },
      "reconfigure_manual": {
        "title": "Refresh Rust+ credentials",
        "description": "Paste the new FCM credentials JSON. This replaces the stored credentials for this account and reconnects the push listener. Your paired devices are kept.",
        "data": {
          "fcm_credentials": "FCM Credentials (JSON string)"
        }
      },
      "confirm_discovery": {
        "title": "Add Rust Server",
        "description": "A Rust server was paired via the Rust+ app. Do you want to add it to Home Assistant?"
      }
    },
    "progress": {
      "awaiting_scan": "Waiting for you to scan the Steam QR code and approve the sign-in in the Steam Mobile app. This window stays open for a few minutes, then registers for push notifications automatically."
    },
    "error": {
      "cannot_connect": "Failed to connect",
      "invalid_auth": "Invalid authentication",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "Device is already configured",
      "qr_begin_failed": "Couldn't start the Steam sign-in. Please try again.",
      "reconfigure_successful": "Credentials updated. Your paired devices were kept.",
      "reconfigure_account_only": "Only the Rust+ account entry holds FCM credentials. Reconfigure the account entry, not a server."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Rust+ Options",
        "menu_options": {
          "add_camera": "Add a camera",
          "remove_camera": "Remove a camera",
          "chat_settings": "Team-chat command settings"
        }
      },
      "add_camera": {
        "title": "Add a Rust+ camera",
        "description": "Enter the camera identifier you set at an in-game Computer Station (e.g. `OILRIG1`). The type is auto-detected, but you can force it: `cctv` = view only, `ptz` = view + aim (no fire), `turret` = view + aim + fire.",
        "data": {
          "camera_id": "Camera identifier",
          "name": "Friendly name (optional)",
          "camera_type": "Type (auto / cctv / turret)"
        }
      },
      "remove_camera": {
        "title": "Remove Rust+ cameras",
        "data": {
          "cameras": "Cameras to remove"
        }
      },
      "chat_settings": {
        "title": "Team-chat commands",
        "description": "Messages in team chat starting with this prefix fire the `rustplus_command` event in Home Assistant (e.g. `!lights on`).",
        "data": {
          "command_prefix": "Command prefix"
        }
      }
    },
    "error": {
      "camera_not_found": "No camera with that identifier is accessible. Check the ID, and that the camera or turret is powered and within range.",
      "unknown": "Unexpected error while validating the camera."
    },
    "abort": {
      "not_a_server": "Camera options are only available on Rust+ server entries."
    }
  },
  "issues": {
    "device_paired": {
      "title": "Rust+ Device Paired: {name}",
      "description": "A new {type} ({name}, ID: {id}) was paired via the Rust+ app, but it couldn't be matched to a configured server yet. Make sure the server is added to Home Assistant and loaded, then re-pair the device — it will be added automatically."
    },
    "rename_device": {
      "title": "Action Required: Rename {name}",
      "description": "You successfully paired the {type} **{name}** (ID: {id}).\n\nBy default, Facepunch only provides generic names like 'Smart Alarm' during pairing.\n\n**Please go to your Home Assistant Devices dashboard, click on this newly paired entity, and rename it to a friendly name (like 'Front Door Alarm') so you can easily identify it!**"
    },
    "device_destroyed": {
      "title": "Rust+ device destroyed: {name}",
      "fix_flow": {
        "step": {
          "confirm": {
            "title": "Remove destroyed device?",
            "description": "**{name}** was destroyed in-game and is no longer reachable, so its entities are unavailable.\n\nRemove it and its entities from Home Assistant? If you rebuild and re-pair it in the Rust+ app, it'll be added back automatically."
          }
        }
      }
    }
  }
}